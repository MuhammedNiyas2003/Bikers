version: '3.8'

services:
  motoescape-app:
    build: .
    container_name: motoescape-app
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      - PORT=5000
      - DATABASE_URL=${DATABASE_URL}
      - NODE_ENV=production
    restart: always
